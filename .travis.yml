language: python
cache: pip

python:
  - "3.12"

services:
  - postgresql

env:
  global:
    - DJANGO_SETTINGS_MODULE=duesanddos.settings
    - PGHOST=127.0.0.1
    - PGPORT=5432
    - PGUSER=postgres
    - PGPASSWORD=
    - PGDATABASE=travis_ci_test

install:
  - pip install -r duesanddos/requirements.txt
  - pip install coverage 'coveralls[yaml]' flake8 black

before_script:
  - psql -h 127.0.0.1 -U postgres -d postgres -c 'CREATE DATABASE travis_ci_test;'
  - cd duesanddos

script:
  - black --check .
  - flake8 .
  - coverage run manage.py test -v 2

after_success:
  - coveralls --verbose

before_deploy:
  - echo "AWS_APP=$AWS_APP"
  - echo "AWS_ENV_MAIN=$AWS_ENV_MAIN"
  - echo "AWS_ENV_DEVELOP=$AWS_ENV_DEVELOP"
  - echo "AWS_S3_REGION_NAME=$AWS_S3_REGION_NAME"
  - echo "AWS_DEPLOYMENT_BUCKET=$AWS_DEPLOYMENT_BUCKET"

deploy:
  - provider: elasticbeanstalk
    access_key_id: "$AWS_ACCESS_KEY_ID"
    secret_access_key: "$AWS_SECRET_ACCESS_KEY"
    region: "$AWS_S3_REGION_NAME"
    app: "$AWS_APP"
    env: "$AWS_ENV_DEVELOP"
    bucket: "$AWS_DEPLOYMENT_BUCKET"
    edge: true
    on:
      branch: develop
      python: "3.12"

  - provider: elasticbeanstalk
    access_key_id: "$AWS_ACCESS_KEY_ID"
    secret_access_key: "$AWS_SECRET_ACCESS_KEY"
    region: "$AWS_S3_REGION_NAME"
    app: "$AWS_APP"
    env: "$AWS_ENV_MAIN"
    bucket: "$AWS_DEPLOYMENT_BUCKET"
    edge: true
    on:
      branch: main
      python: "3.12"